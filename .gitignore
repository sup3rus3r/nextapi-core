node_modules
package-lock.json
__pycache__
app.db

# real secrets, never commit
.env
.env.local
backend/.env
frontend/.env.local

# CLI local state: auth.json holds a live client_id/client_secret,
# modules/ is a staged-source cache re-fetchable from the registry.
# modules.lock.json is NOT ignored -it's the real dependency manifest,
# same as package-lock.json, and should be committed.
.nextapi/auth.json
.nextapi/modules/
.nextapi/capabilities.local.json